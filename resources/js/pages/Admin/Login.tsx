import { Head, useForm } from "@inertiajs/react";

export default function Login() {
  const form = useForm({ email: "", password: "", remember: true });
  const inputClass = "mt-2 w-full rounded-xl border border-input bg-background p-3 text-navy outline-none transition focus:border-primary focus:ring-2 focus:ring-primary/15";

  return <><Head title="Admin Login" /><main className="admin-light flex min-h-screen items-center justify-center bg-surface p-4"><form onSubmit={(event) => { event.preventDefault(); form.post("/admin/login"); }} className="w-full max-w-md rounded-3xl bg-card p-6 shadow-xl sm:p-8"><img src="/images/easy-blue-divers-zanzibar-logo.png" alt="Easy Blue Divers" className="mx-auto size-20 rounded-full" /><h1 className="mt-5 text-center text-2xl font-extrabold text-navy">Easy Blue Admin</h1><p className="mt-2 text-center text-sm text-muted-foreground">Sign in to manage bookings and website content.</p><label className="mt-7 block text-sm font-bold text-navy">Email<input type="email" autoComplete="email" value={form.data.email} onChange={(event) => form.setData("email", event.target.value)} className={inputClass} required /></label>{form.errors.email ? <p className="mt-1.5 text-sm font-medium text-destructive">{form.errors.email}</p> : null}<label className="mt-4 block text-sm font-bold text-navy">Password<input type="password" autoComplete="current-password" value={form.data.password} onChange={(event) => form.setData("password", event.target.value)} className={inputClass} required /></label><label className="mt-4 inline-flex cursor-pointer items-center gap-2 text-sm font-semibold text-navy"><input type="checkbox" checked={form.data.remember} onChange={(event) => form.setData("remember", event.target.checked)} className="size-4 accent-primary" />Keep me signed in on this device</label><button disabled={form.processing} className="mt-6 w-full rounded-full bg-primary p-3 font-bold text-primary-foreground disabled:cursor-wait disabled:opacity-60">{form.processing ? "Signing in…" : "Sign in"}</button></form></main></>;
}
