import { Link, router, useForm, usePage } from "@inertiajs/react";
import { Eye, Pencil, Trash2 } from "lucide-react";
import { useState } from "react";
import { AdminLayout } from "@/components/AdminLayout";
import { AdminAddPanel } from "@/components/AdminAddPanel";

type Category = { id: number; name: string };
type Item = { id: number; title: string; slug: string; description: string; experience_category_id?: number | null; experience_category?: Category | null; sort_order?: number; duration?: string | null; level?: string | null; price_from?: string | null; image_path?: string | null; highlights?: string[] | null; included_items?: string[] | null; requirements?: string[] | null; meeting_info?: string | null; cancellation_note?: string | null; seo_title?: string | null; seo_description?: string | null; is_active: boolean };
type ExperienceData = { title: string; experience_category_id: string; sort_order: string; description: string; duration: string; level: string; price_from: string; image: File | null; highlights: string; included_items: string; requirements: string; meeting_info: string; cancellation_note: string; seo_title: string; seo_description: string; is_active: boolean };
const field = "mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 text-sm focus:border-primary focus:outline-none focus:ring-2 focus:ring-primary/20";
const blank: ExperienceData = { title: "", experience_category_id: "", sort_order: "0", description: "", duration: "", level: "", price_from: "", image: null, highlights: "", included_items: "", requirements: "", meeting_info: "", cancellation_note: "", seo_title: "", seo_description: "", is_active: true };
const toLines = (items?: string[] | null) => items?.join("\n") ?? "";

function DetailFields({ form, categories, imageRequired = false }: { form: ReturnType<typeof useForm<ExperienceData>>; categories: Category[]; imageRequired?: boolean }) {
  return <div className="grid gap-5 md:grid-cols-2"><label className="text-sm font-bold text-navy">Experience name<input required placeholder="e.g. Discover Scuba Diving" value={form.data.title} onChange={(event) => form.setData("title", event.target.value)} className={field} /></label><label className="text-sm font-bold text-navy">CMS category<select value={form.data.experience_category_id} onChange={(event) => form.setData("experience_category_id", event.target.value)} className={field}><option value="">Select category</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label className="text-sm font-bold text-navy">Display order <span className="font-normal text-muted-foreground">(lower appears first)</span><input type="number" min="0" max="9999" value={form.data.sort_order} onChange={(event) => form.setData("sort_order", event.target.value)} className={field} /></label><label className="text-sm font-bold text-navy">Cover image <span className="font-normal text-muted-foreground">(JPG, PNG or WEBP · max 5 MB)</span><input required={imageRequired} type="file" accept="image/jpeg,image/png,image/webp" onChange={(event) => form.setData("image", event.target.files?.[0] ?? null)} className={field} /></label><label className="text-sm font-bold text-navy">Duration<input placeholder="e.g. Half day" value={form.data.duration} onChange={(event) => form.setData("duration", event.target.value)} className={field} /></label><label className="text-sm font-bold text-navy">Starting price<input placeholder="e.g. From USD 120" value={form.data.price_from} onChange={(event) => form.setData("price_from", event.target.value)} className={field} /></label><label className="text-sm font-bold text-navy">Diver level<input placeholder="e.g. Beginner" value={form.data.level} onChange={(event) => form.setData("level", event.target.value)} className={field} /></label><div className="hidden md:block" /><label className="text-sm font-bold text-navy md:col-span-2">Description<textarea required value={form.data.description} onChange={(event) => form.setData("description", event.target.value)} placeholder="Explain what the guest will experience and why it is right for them." className={`${field} min-h-32`} /></label><label className="text-sm font-bold text-navy">Highlights <span className="font-normal text-muted-foreground">(one per line)</span><textarea value={form.data.highlights} onChange={(event) => form.setData("highlights", event.target.value)} placeholder="Beginner friendly&#10;Small group" className={`${field} min-h-28`} /></label><label className="text-sm font-bold text-navy">Included <span className="font-normal text-muted-foreground">(one per line)</span><textarea value={form.data.included_items} onChange={(event) => form.setData("included_items", event.target.value)} placeholder="Professional guide&#10;Diving equipment" className={`${field} min-h-28`} /></label><label className="text-sm font-bold text-navy">Requirements <span className="font-normal text-muted-foreground">(one per line)</span><textarea value={form.data.requirements} onChange={(event) => form.setData("requirements", event.target.value)} placeholder="Minimum age&#10;Medical suitability" className={`${field} min-h-28`} /></label><label className="text-sm font-bold text-navy">Meeting / pickup information<textarea value={form.data.meeting_info} onChange={(event) => form.setData("meeting_info", event.target.value)} className={`${field} min-h-28`} /></label><label className="text-sm font-bold text-navy md:col-span-2">Cancellation note<textarea value={form.data.cancellation_note} onChange={(event) => form.setData("cancellation_note", event.target.value)} className={`${field} min-h-24`} /></label><label className="text-sm font-bold text-navy">SEO title <span className="font-normal text-muted-foreground">({form.data.seo_title.length}/255)</span><input value={form.data.seo_title} onChange={(event) => form.setData("seo_title", event.target.value)} placeholder="Diving experience | Easy Blue Divers" className={field} /></label><label className="text-sm font-bold text-navy">SEO description <span className="font-normal text-muted-foreground">({form.data.seo_description.length}/500)</span><textarea value={form.data.seo_description} onChange={(event) => form.setData("seo_description", event.target.value)} className={`${field} min-h-24`} /></label></div>;
}

function ExperienceEditor({ item, categories }: { item: Item; categories: Category[] }) {
  const [editing, setEditing] = useState(false);
  const form = useForm<ExperienceData>({ title: item.title, experience_category_id: item.experience_category_id?.toString() ?? "", sort_order: String(item.sort_order ?? 0), description: item.description, duration: item.duration ?? "", level: item.level ?? "", price_from: item.price_from ?? "", image: null, highlights: toLines(item.highlights), included_items: toLines(item.included_items), requirements: toLines(item.requirements), meeting_info: item.meeting_info ?? "", cancellation_note: item.cancellation_note ?? "", seo_title: item.seo_title ?? "", seo_description: item.seo_description ?? "", is_active: item.is_active });
  if (!editing) return <article className="flex flex-col gap-4 rounded-2xl bg-card p-5 shadow-sm sm:flex-row sm:items-center sm:justify-between"><div className="flex min-w-0 items-center gap-4">{item.image_path ? <img src={`/storage/${item.image_path}`} alt="" className="size-14 rounded-xl object-cover" /> : null}<div><b className="text-navy">{item.title}</b><p className="mt-1 text-sm text-muted-foreground">{item.duration || "Duration not set"} · {item.level || "Level not set"} · {item.price_from || "Price on request"}</p><p className="mt-1 text-xs text-muted-foreground">Order {item.sort_order ?? 0}{item.experience_category?.name ? ` · ${item.experience_category.name}` : ""}</p><p className={`mt-1 text-xs font-bold ${item.is_active ? "text-emerald-600" : "text-muted-foreground"}`}>{item.is_active ? "Published" : "Unpublished"}</p></div></div><div className="flex flex-wrap gap-2"><Link href={`/diving/experience/${item.slug}`} target="_blank" className="inline-flex items-center gap-1.5 rounded-full border border-border px-3 py-2 text-xs font-bold text-navy"><Eye className="size-3.5" />Preview</Link><button onClick={() => setEditing(true)} className="inline-flex items-center gap-1.5 rounded-full bg-primary px-3 py-2 text-xs font-bold text-primary-foreground"><Pencil className="size-3.5" />Edit</button><button onClick={() => { if (window.confirm(`Delete “${item.title}”? This cannot be undone.`)) router.delete(`/admin/experiences/${item.id}`); }} className="rounded-full p-2 text-destructive hover:bg-destructive/10" aria-label={`Delete ${item.title}`}><Trash2 className="size-4" /></button></div></article>;
  return <article className="rounded-3xl border border-primary/30 bg-card p-6 shadow-sm"><div className="flex items-center justify-between gap-4"><h2 className="text-lg font-extrabold text-navy">Edit: {item.title}</h2><button onClick={() => setEditing(false)} className="text-sm font-bold text-muted-foreground">Close</button></div><form className="mt-6" onSubmit={(event) => { event.preventDefault(); form.put(`/admin/experiences/${item.id}`, { forceFormData: true, onSuccess: () => setEditing(false) }); }}><DetailFields form={form} categories={categories} /><label className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-navy"><input type="checkbox" checked={form.data.is_active} onChange={(event) => form.setData("is_active", event.target.checked)} className="size-4 accent-primary" /> Publish this experience</label><div className="mt-6 flex gap-3"><button disabled={form.processing} className="rounded-full bg-primary px-6 py-3 text-sm font-bold text-primary-foreground disabled:opacity-60">{form.processing ? "Saving…" : "Save changes"}</button><button type="button" onClick={() => setEditing(false)} className="rounded-full border border-border px-6 py-3 text-sm font-bold text-navy">Cancel</button></div></form></article>;
}

export default function Experiences() {
  const { experiences, categories } = usePage<{ experiences: Item[]; categories: Category[] }>().props;
  const form = useForm<ExperienceData>(blank);
  return <AdminLayout title="Diving Experiences"><AdminAddPanel title="Add a diving experience" description="Create the full public product page, then attach departure slots."><form onSubmit={(event) => { event.preventDefault(); form.post("/admin/experiences", { forceFormData: true, onSuccess: () => form.reset() }); }}><DetailFields form={form} categories={categories} imageRequired /><button disabled={form.processing} className="mt-6 rounded-full bg-primary px-6 py-3 font-bold text-primary-foreground disabled:opacity-60">{form.processing ? "Saving…" : "Save experience"}</button></form></AdminAddPanel><section className="mt-8 space-y-4"><h2 className="text-xl font-extrabold text-navy">Manage experiences</h2>{experiences.map((item) => <ExperienceEditor key={item.id} item={item} categories={categories} />)}{!experiences.length ? <p className="rounded-2xl bg-card p-6 text-sm text-muted-foreground">No experiences added yet.</p> : null}</section></AdminLayout>;
}
